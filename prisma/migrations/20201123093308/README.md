# Migration `20201123093308`

This migration has been generated by Max Streitberger at 11/23/2020, 10:33:08 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Event" (
    "event_id" TEXT NOT NULL,
    "title" TEXT NOT NULL,
    "event_start" TEXT NOT NULL,
    "description" TEXT,
    "is_public" BOOLEAN NOT NULL,
    "event_type" "EventType" NOT NULL,
    "street_and_house_number" TEXT,
    "city" TEXT,
    "country" TEXT,
    "meeting_link" TEXT,
    "created_at" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
    "host_id" TEXT NOT NULL,
    "zip" TEXT,
    "event_color" TEXT NOT NULL DEFAULT E'green',
    "date" TIMESTAMP(3) NOT NULL,

    PRIMARY KEY ("event_id")
)

CREATE TABLE "User" (
    "user_id" TEXT NOT NULL,
    "first_name" TEXT,
    "last_name" TEXT,
    "username" TEXT,
    "email" TEXT,
    "password" TEXT,
    "role" "Role",
    "created_at" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
    "last_login" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
    "confirmed" BOOLEAN NOT NULL DEFAULT false,
    "profile_color" TEXT DEFAULT E'blue',

    PRIMARY KEY ("user_id")
)

CREATE TABLE "User_Event" (
    "event_id" TEXT NOT NULL,
    "user_id" TEXT NOT NULL,
    "joined_at" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,

    PRIMARY KEY ("event_id","user_id")
)

CREATE TABLE "EventTask" (
    "event_id" TEXT,
    "title" TEXT,
    "description" TEXT,
    "deadline" TEXT,
    "created_at" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
    "user_id" TEXT,
    "event_task_id" TEXT NOT NULL,

    PRIMARY KEY ("event_task_id")
)

CREATE TABLE "PrivateTask" (
    "user_id" TEXT,
    "title" TEXT,
    "description" TEXT,
    "deadline" TEXT,
    "created_at" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
    "private_task_id" TEXT NOT NULL,

    PRIMARY KEY ("private_task_id")
)

CREATE TABLE "Friend" (
    "follows_since" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
    "following_user_id" TEXT NOT NULL,
    "friend_id" TEXT NOT NULL,

    PRIMARY KEY ("friend_id","following_user_id")
)

CREATE TABLE "Survey" (
    "survey_id" TEXT NOT NULL,
    "event_id" TEXT,
    "question" TEXT,
    "created_at" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,

    PRIMARY KEY ("survey_id")
)

CREATE TABLE "SurveyAnswer" (
    "answer_id" TEXT NOT NULL,
    "survey_id" TEXT,
    "answer" TEXT,
    "created_at" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
    "likes" INTEGER DEFAULT 0,

    PRIMARY KEY ("answer_id")
)

CREATE TABLE "Vote" (
    "user_id" TEXT NOT NULL,
    "survey_id" TEXT NOT NULL,
    "voted_at" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
    "answer_id" TEXT NOT NULL,

    PRIMARY KEY ("survey_id","user_id")
)

CREATE UNIQUE INDEX "User.email_unique" ON "User"("email")

ALTER TABLE "Event" ADD FOREIGN KEY("host_id")REFERENCES "User"("user_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "User_Event" ADD FOREIGN KEY("event_id")REFERENCES "Event"("event_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "User_Event" ADD FOREIGN KEY("user_id")REFERENCES "User"("user_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "EventTask" ADD FOREIGN KEY("event_id")REFERENCES "Event"("event_id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "EventTask" ADD FOREIGN KEY("user_id")REFERENCES "User"("user_id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "PrivateTask" ADD FOREIGN KEY("user_id")REFERENCES "User"("user_id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "Friend" ADD FOREIGN KEY("following_user_id")REFERENCES "User"("user_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Friend" ADD FOREIGN KEY("friend_id")REFERENCES "User"("user_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Survey" ADD FOREIGN KEY("event_id")REFERENCES "Event"("event_id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "SurveyAnswer" ADD FOREIGN KEY("survey_id")REFERENCES "Survey"("survey_id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "Vote" ADD FOREIGN KEY("answer_id")REFERENCES "SurveyAnswer"("answer_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Vote" ADD FOREIGN KEY("survey_id")REFERENCES "Survey"("survey_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Vote" ADD FOREIGN KEY("user_id")REFERENCES "User"("user_id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201123093308
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,136 @@
+generator client {
+  provider      = "prisma-client-js"
+  binaryTargets = ["native", "linux-musl"]
+}
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+model Event {
+  event_id                String       @id @default(cuid())
+  title                   String
+  event_start             String
+  description             String?
+  is_public               Boolean
+  event_type              EventType
+  street_and_house_number String?
+  city                    String?
+  country                 String?
+  meeting_link            String?
+  created_at              DateTime?    @default(now())
+  host_id                 String
+  zip                     String?
+  event_color             String       @default("green")
+  date                    DateTime
+  host                    User         @relation(fields: [host_id], references: [user_id])
+  tasks                   EventTask[]
+  surveys                 Survey[]
+  guests                  User_Event[]
+}
+
+model User {
+  user_id       String        @id @default(cuid())
+  first_name    String?
+  last_name     String?
+  username      String?
+  email         String?       @unique
+  password      String?
+  role          Role?
+  created_at    DateTime?     @default(now())
+  last_login    DateTime?     @default(now())
+  confirmed     Boolean       @default(false)
+  profile_color String?       @default("blue")
+  events        Event[]
+  event_tasks   EventTask[]
+  friends       Friend[]      @relation("Friend_following_user_idToUser")
+  followed_by   Friend[]      @relation("Friend_friend_idToUser")
+  private_tasks PrivateTask[]
+  joinedEvents  User_Event[]
+  votes         Vote[]
+}
+
+model User_Event {
+  event_id  String
+  user_id   String
+  joined_at DateTime? @default(now())
+  event     Event     @relation(fields: [event_id], references: [event_id])
+  user      User      @relation(fields: [user_id], references: [user_id])
+
+  @@id([event_id, user_id])
+}
+
+model EventTask {
+  event_id      String?
+  title         String?
+  description   String?
+  deadline      String?
+  created_at    DateTime? @default(now())
+  user_id       String?
+  event_task_id String    @id @default(cuid())
+  event         Event?    @relation(fields: [event_id], references: [event_id])
+  created_by    User?     @relation(fields: [user_id], references: [user_id])
+}
+
+model PrivateTask {
+  user_id         String?
+  title           String?
+  description     String?
+  deadline        String?
+  created_at      DateTime? @default(now())
+  private_task_id String    @id @default(cuid())
+  created_by      User?     @relation(fields: [user_id], references: [user_id])
+}
+
+model Friend {
+  follows_since     DateTime? @default(now())
+  following_user_id String
+  friend_id         String
+  following_user    User      @relation("Friend_following_user_idToUser", fields: [following_user_id], references: [user_id])
+  friend            User      @relation("Friend_friend_idToUser", fields: [friend_id], references: [user_id])
+
+  @@id([friend_id, following_user_id])
+}
+
+model Survey {
+  survey_id  String         @id @default(cuid())
+  event_id   String?
+  question   String?
+  created_at DateTime?      @default(now())
+  event      Event?         @relation(fields: [event_id], references: [event_id])
+  answers    SurveyAnswer[]
+  votes      Vote[]
+}
+
+model SurveyAnswer {
+  answer_id  String    @id @default(cuid())
+  survey_id  String?
+  answer     String?
+  created_at DateTime? @default(now())
+  likes      Int?      @default(0)
+  survey     Survey?   @relation(fields: [survey_id], references: [survey_id])
+  votes      Vote[]
+}
+
+model Vote {
+  user_id   String
+  survey_id String
+  voted_at  DateTime?    @default(now())
+  answer_id String
+  answer    SurveyAnswer @relation(fields: [answer_id], references: [answer_id])
+  survey    Survey       @relation(fields: [survey_id], references: [survey_id])
+  user      User         @relation(fields: [user_id], references: [user_id])
+
+  @@id([survey_id, user_id])
+}
+
+enum EventType {
+  LOCAL
+  ONLINE
+}
+
+enum Role {
+  ADMIN
+  USER
+}
```



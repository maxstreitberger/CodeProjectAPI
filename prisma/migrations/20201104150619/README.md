# Migration `20201104150619`

This migration has been generated by Max Streitberger at 11/4/2020, 4:06:19 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Vote" DROP COLUMN "event_id"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201104150017..20201104150619
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Event {
   event_id                String       @id @default(cuid())
@@ -108,9 +108,8 @@
 model Vote {
   user_id     String
   survey_id   String
-  event_id    String
   voted_at    DateTime?       @default(now())
   survey      Survey          @relation(fields: [survey_id], references: [survey_id])
   user        User            @relation(fields: [user_id], references: [user_id])
   answer_id   String
```



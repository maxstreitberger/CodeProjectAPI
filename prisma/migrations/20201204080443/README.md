# Migration `20201204080443`

This migration has been generated by Max Streitberger at 12/4/2020, 9:04:43 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201204075510..20201204080443
--- datamodel.dml
+++ datamodel.dml
@@ -4,9 +4,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model cities {
   name       String    @id
@@ -48,14 +48,14 @@
   host_id                 String
   zip                     Int?
   event_color             String?       @default("green")
   date                    DateTime?
-  cities                  cities?       @relation(fields: [city_id], references: [name])
-  countries               countries?    @relation(fields: [country_id], references: [name])
+  city                    cities?       @relation(fields: [city_id], references: [name])
+  country                 countries?    @relation(fields: [country_id], references: [name])
   host                    users         @relation(fields: [host_id], references: [user_id])
-  event_tasks             event_tasks[]
+  tasks                   event_tasks[]
   surveys                 surveys[]
-  user_event              user_event[]
+  guests                  user_event[]
   @@index([title], name: "event_title_idx")
 }
```



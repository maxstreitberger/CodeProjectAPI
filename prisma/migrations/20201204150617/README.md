# Migration `20201204150617`

This migration has been generated by Max Streitberger at 12/4/2020, 4:06:17 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "survey_answers" DROP COLUMN "answer",
ADD COLUMN     "title" TEXT
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201204132148..20201204150617
--- datamodel.dml
+++ datamodel.dml
@@ -4,9 +4,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model cities {
   name       String    @id
@@ -53,9 +53,9 @@
   country                 countries?    @relation(fields: [country_id], references: [name])
   host                    users         @relation(fields: [host_id], references: [user_id])
   tasks                   event_tasks[]
   surveys                 surveys[]
-  guests                  user_event[]
+  invited_users           user_event[]
   @@index([title], name: "event_title_idx")
 }
@@ -82,9 +82,9 @@
 model survey_answers {
   answer_id  String    @id @default(cuid())
   survey_id  String
-  answer     String?
+  title      String?
   created_at DateTime? @default(now())
   survey     surveys   @relation(fields: [survey_id], references: [survey_id])
   votes      votes[]
 }
@@ -125,9 +125,9 @@
   event_tasks                              event_tasks[]
   events                                   events[]
   following_users                          friends[]       @relation("friends_following_user_idTousers")
   friends                                  friends[]       @relation("friends_friend_idTousers")
-  private_tasks                            private_tasks[]
+  my_tasks                                 private_tasks[]
   joined_events                            user_event[]
   votes                                    votes[]
   @@index([first_name], name: "first_name_idx")
@@ -140,9 +140,9 @@
   voted_at       DateTime?      @default(now())
   answer_id      String
   answer         survey_answers @relation(fields: [answer_id], references: [answer_id])
   survey         surveys        @relation(fields: [survey_id], references: [survey_id])
-  user           users          @relation(fields: [user_id], references: [user_id])
+  who_voted      users          @relation(fields: [user_id], references: [user_id])
   @@id([survey_id, user_id])
 }
```



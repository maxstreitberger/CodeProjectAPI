# Migration `20201204075118`

This migration has been generated by Max Streitberger at 12/4/2020, 8:51:18 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "event_tasks" ADD COLUMN     "done" BOOLEAN DEFAULT false
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201204074344..20201204075118
--- datamodel.dml
+++ datamodel.dml
@@ -4,9 +4,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model cities {
   name       String    @id
@@ -27,10 +27,11 @@
   description   String?
   deadline      DateTime?
   created_at    DateTime? @default(now())
   user_id       String
+  done          Boolean?  @default(false)
   events        events    @relation(fields: [event_id], references: [event_id])
-  users         users     @relation(fields: [user_id], references: [user_id])
+  created_by    users     @relation(fields: [user_id], references: [user_id])
 }
 model events {
   event_id                String        @id @default(cuid())
```



# Migration `20201029095503`

This migration has been generated by Max Streitberger at 10/29/2020, 10:55:03 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."PrivateTask" DROP CONSTRAINT "PrivateTask_pkey",
DROP COLUMN "task_id",
ADD COLUMN "private_task_id" text   NOT NULL ,
ADD PRIMARY KEY ("private_task_id")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201029094818..20201029095503
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Event {
   event_id                String       @id @default(cuid())
@@ -52,27 +52,27 @@
   @@id([event_id, user_id])
 }
 model EventTask {
+  task_id       String      @id @default(cuid())
   event_id      String?
   title         String?
   description   String?
   deadline      String?
   created_at    DateTime?   @default(now())
   user_id       String?
-  task_id       String      @id @default(cuid())
   event         Event?      @relation(fields: [event_id], references: [event_id])
   created_by    User?       @relation(fields: [user_id], references: [user_id])
 }
 model PrivateTask {
-  task_id           String      @id @default(cuid())
-  user_id           String?
-  title             String?
-  description       String?
-  deadline          String?
-  created_at        DateTime?   @default(now())
-  created_by        User?       @relation(fields: [user_id], references: [user_id])
+  private_task_id           String          @id @default(cuid())
+  user_id                   String?
+  title                     String?
+  description               String?
+  deadline                  String?
+  created_at                DateTime?       @default(now())
+  created_by                User?           @relation(fields: [user_id], references: [user_id])
 }
 enum EventType {
   LOCAL
```



# Migration `20201104150706`

This migration has been generated by Max Streitberger at 11/4/2020, 4:07:06 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Vote" DROP CONSTRAINT "Vote_answer_id_fkey"

ALTER TABLE "public"."Vote" DROP COLUMN "answer_id"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201104150619..20201104150706
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Event {
   event_id                String       @id @default(cuid())
@@ -111,10 +111,10 @@
   survey_id   String
   voted_at    DateTime?       @default(now())
   survey      Survey          @relation(fields: [survey_id], references: [survey_id])
   user        User            @relation(fields: [user_id], references: [user_id])
-  answer_id   String
-  answer      SurveyAnswer    @relation(fields: [answer_id], references: [answer_id])
+  // answer_id   String
+  // answer      SurveyAnswer    @relation(fields: [answer_id], references: [answer_id])
   @@id([survey_id, user_id])
 }
```



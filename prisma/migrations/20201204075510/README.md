# Migration `20201204075510`

This migration has been generated by Max Streitberger at 12/4/2020, 8:55:10 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "event_tasks" ALTER COLUMN "deadline" SET NOT NULL

ALTER TABLE "private_tasks" ALTER COLUMN "title" SET NOT NULL,
ALTER COLUMN "deadline" SET NOT NULL
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201204075118..20201204075510
--- datamodel.dml
+++ datamodel.dml
@@ -4,9 +4,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model cities {
   name       String    @id
@@ -24,9 +24,9 @@
   event_task_id String    @id @default(cuid())
   event_id      String
   title         String
   description   String?
-  deadline      DateTime?
+  deadline      DateTime
   created_at    DateTime? @default(now())
   user_id       String
   done          Boolean?  @default(false)
   events        events    @relation(fields: [event_id], references: [event_id])
@@ -71,11 +71,11 @@
 model private_tasks {
   private_task_id String    @id @default(cuid())
   user_id         String
-  title           String?
+  title           String
   description     String?
-  deadline        DateTime?
+  deadline        DateTime
   created_at      DateTime? @default(now())
   done            Boolean?  @default(false)
   created_by      users     @relation(fields: [user_id], references: [user_id])
 }
```



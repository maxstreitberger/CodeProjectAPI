# Migration `20201023132418`

This migration has been generated by Max Streitberger at 10/23/2020, 3:24:18 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Event" DROP COLUMN "date",
ADD COLUMN "date" text   ,
DROP COLUMN "event_start",
ADD COLUMN "event_start" text   
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201023132313..20201023132418
--- datamodel.dml
+++ datamodel.dml
@@ -3,16 +3,16 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Event {
   event_id                String      @id @default(cuid())
   title                   String?
-  date                    DateTime?   @default(dbgenerated())
-  event_start             DateTime?   @default(dbgenerated())
+  date                    String?   @default(dbgenerated())
+  event_start             String?   @default(dbgenerated())
   description             String?
   is_public               Boolean?
   event_type              event_type?
   street_and_house_number String?
```



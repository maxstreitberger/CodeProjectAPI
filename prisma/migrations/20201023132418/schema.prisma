generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url = "***"
}

model Event {
  event_id                String      @id @default(cuid())
  title                   String?
  date                    String?   @default(dbgenerated())
  event_start             String?   @default(dbgenerated())
  description             String?
  is_public               Boolean?
  event_type              event_type?
  street_and_house_number String?
  city                    String?
  country                 String?
  meeting_link            String?
  created_at              DateTime?   @default(now())
  host_id                 String?
  User                    User?       @relation(fields: [host_id], references: [user_id])
}

model User {
  user_id    String    @id @default(cuid())
  first_name String?
  last_name  String?
  username   String?
  email      String?   @unique
  password   String?
  role       role?
  created_at DateTime? @default(now())
  last_login DateTime? @default(now())
  Event      Event[]
}

enum event_type {
  LOCAL
  ONLINE
}

enum role {
  ADMIN
  USER
}

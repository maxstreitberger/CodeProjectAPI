# Migration `20201104094240`

This migration has been generated by Max Streitberger at 11/4/2020, 10:42:40 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Friend" DROP CONSTRAINT "Friend_follower_id_fkey"

ALTER TABLE "public"."Friend" DROP CONSTRAINT "Friend_user_id_fkey"

ALTER TABLE "public"."Friend" DROP CONSTRAINT "user_follower_id",
DROP COLUMN "user_id",
DROP COLUMN "follower_id",
ADD COLUMN "following_user_id" text   NOT NULL ,
ADD COLUMN "friend_id" text   NOT NULL ,
ADD PRIMARY KEY ("friend_id", "following_user_id")

ALTER TABLE "public"."Friend" ADD FOREIGN KEY("friend_id")REFERENCES "public"."User"("user_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Friend" ADD FOREIGN KEY("following_user_id")REFERENCES "public"."User"("user_id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201104093140..20201104094240
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Event {
   event_id                String       @id @default(cuid())
@@ -76,15 +76,15 @@
   created_by      User?     @relation(fields: [user_id], references: [user_id])
 }
 model Friend {
-  user_id                       String
-  follower_id                   String
+  following_user_id             String
+  friend_id                     String
   follows_since                 DateTime? @default(now())
-  friend                        User      @relation("Friend_friend_idToUser", fields: [follower_id], references: [user_id])
-  following_user                User      @relation("Friend_following_user_idToUser", fields: [user_id], references: [user_id])
+  friend                        User      @relation("Friend_friend_idToUser", fields: [friend_id], references: [user_id])
+  following_user                User      @relation("Friend_following_user_idToUser", fields: [following_user_id], references: [user_id])
-  @@id([user_id, follower_id])
+  @@id([friend_id, following_user_id])
 }
 enum EventType {
   LOCAL
```



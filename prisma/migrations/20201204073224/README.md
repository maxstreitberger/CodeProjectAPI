# Migration `20201204073224`

This migration has been generated by Max Streitberger at 12/4/2020, 8:32:24 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "cities" (
    "name" TEXT NOT NULL,
    "created_at" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,

    PRIMARY KEY ("name")
)

CREATE TABLE "countries" (
    "name" TEXT NOT NULL,
    "created_at" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,

    PRIMARY KEY ("name")
)

CREATE TABLE "event_tasks" (
    "event_task_id" TEXT NOT NULL,
    "event_id" TEXT NOT NULL,
    "title" TEXT NOT NULL,
    "description" TEXT,
    "deadline" TIMESTAMP(3),
    "created_at" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
    "user_id" TEXT NOT NULL,

    PRIMARY KEY ("event_task_id")
)

CREATE TABLE "events" (
    "event_id" TEXT NOT NULL,
    "title" TEXT NOT NULL,
    "event_start" TEXT,
    "description" TEXT,
    "is_public" BOOLEAN DEFAULT false,
    "event_type" "EventType",
    "street_and_house_number" TEXT,
    "city_id" TEXT,
    "country_id" TEXT,
    "meeting_link" TEXT,
    "created_at" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
    "host_id" TEXT NOT NULL,
    "zip" INTEGER,
    "event_color" TEXT DEFAULT E'green',
    "date" TIMESTAMP(3),

    PRIMARY KEY ("event_id")
)

CREATE TABLE "friends" (
    "follows_since" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
    "following_user_id" TEXT NOT NULL,
    "friend_id" TEXT NOT NULL,

    PRIMARY KEY ("friend_id","following_user_id")
)

CREATE TABLE "private_tasks" (
    "private_task_id" TEXT NOT NULL,
    "user_id" TEXT NOT NULL,
    "title" TEXT,
    "description" TEXT,
    "deadline" TIMESTAMP(3),
    "created_at" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
    "done" BOOLEAN DEFAULT false,

    PRIMARY KEY ("private_task_id")
)

CREATE TABLE "survey_answers" (
    "answer_id" TEXT NOT NULL,
    "survey_id" TEXT NOT NULL,
    "answer" TEXT,
    "created_at" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,

    PRIMARY KEY ("answer_id")
)

CREATE TABLE "surveys" (
    "survey_id" TEXT NOT NULL,
    "event_id" TEXT NOT NULL,
    "question" TEXT,
    "created_at" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,

    PRIMARY KEY ("survey_id")
)

CREATE TABLE "user_event" (
    "event_id" TEXT NOT NULL,
    "user_id" TEXT NOT NULL,
    "joined_at" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,

    PRIMARY KEY ("event_id","user_id")
)

CREATE TABLE "users" (
    "user_id" TEXT NOT NULL,
    "first_name" TEXT,
    "last_name" TEXT,
    "username" TEXT NOT NULL,
    "email" TEXT,
    "password" TEXT,
    "role" "Role",
    "created_at" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
    "last_login" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
    "confirmed" BOOLEAN DEFAULT false,
    "profile_color" TEXT DEFAULT E'blue',
    "is_private" BOOLEAN DEFAULT true,

    PRIMARY KEY ("user_id")
)

CREATE TABLE "votes" (
    "user_id" TEXT NOT NULL,
    "survey_id" TEXT NOT NULL,
    "voted_at" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
    "answer_id" TEXT NOT NULL,

    PRIMARY KEY ("survey_id","user_id")
)

CREATE INDEX "event_title_idx" ON "events"("title")

CREATE UNIQUE INDEX "users.username_unique" ON "users"("username")

CREATE UNIQUE INDEX "users.email_unique" ON "users"("email")

CREATE INDEX "first_name_idx" ON "users"("first_name")

CREATE INDEX "last_name_idx" ON "users"("last_name")

ALTER TABLE "event_tasks" ADD FOREIGN KEY("event_id")REFERENCES "events"("event_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "event_tasks" ADD FOREIGN KEY("user_id")REFERENCES "users"("user_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "events" ADD FOREIGN KEY("city_id")REFERENCES "cities"("name") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "events" ADD FOREIGN KEY("country_id")REFERENCES "countries"("name") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "events" ADD FOREIGN KEY("host_id")REFERENCES "users"("user_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "friends" ADD FOREIGN KEY("following_user_id")REFERENCES "users"("user_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "friends" ADD FOREIGN KEY("friend_id")REFERENCES "users"("user_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "private_tasks" ADD FOREIGN KEY("user_id")REFERENCES "users"("user_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "survey_answers" ADD FOREIGN KEY("survey_id")REFERENCES "surveys"("survey_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "surveys" ADD FOREIGN KEY("event_id")REFERENCES "events"("event_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "user_event" ADD FOREIGN KEY("event_id")REFERENCES "events"("event_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "user_event" ADD FOREIGN KEY("user_id")REFERENCES "users"("user_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "votes" ADD FOREIGN KEY("answer_id")REFERENCES "survey_answers"("answer_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "votes" ADD FOREIGN KEY("survey_id")REFERENCES "surveys"("survey_id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "votes" ADD FOREIGN KEY("user_id")REFERENCES "users"("user_id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201204073224
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,156 @@
+generator client {
+  provider      = "prisma-client-js"
+  binaryTargets = ["native", "linux-musl"]
+}
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+model cities {
+  name       String    @id
+  created_at DateTime? @default(now())
+  events     events[]
+}
+
+model countries {
+  name       String    @id
+  created_at DateTime? @default(now())
+  events     events[]
+}
+
+model event_tasks {
+  event_task_id String    @id @default(cuid())
+  event_id      String
+  title         String
+  description   String?
+  deadline      DateTime?
+  created_at    DateTime? @default(now())
+  user_id       String
+  events        events    @relation(fields: [event_id], references: [event_id])
+  users         users     @relation(fields: [user_id], references: [user_id])
+}
+
+model events {
+  event_id                String        @id @default(cuid())
+  title                   String
+  event_start             String?
+  description             String?
+  is_public               Boolean?      @default(false)
+  event_type              EventType?
+  street_and_house_number String?
+  city_id                 String?
+  country_id              String?
+  meeting_link            String?
+  created_at              DateTime?     @default(now())
+  host_id                 String
+  zip                     Int?
+  event_color             String?       @default("green")
+  date                    DateTime?
+  cities                  cities?       @relation(fields: [city_id], references: [name])
+  countries               countries?    @relation(fields: [country_id], references: [name])
+  users                   users         @relation(fields: [host_id], references: [user_id])
+  event_tasks             event_tasks[]
+  surveys                 surveys[]
+  user_event              user_event[]
+
+  @@index([title], name: "event_title_idx")
+}
+
+model friends {
+  follows_since                          DateTime? @default(now())
+  following_user_id                      String
+  friend_id                              String
+  users_friends_following_user_idTousers users     @relation("friends_following_user_idTousers", fields: [following_user_id], references: [user_id])
+  users_friends_friend_idTousers         users     @relation("friends_friend_idTousers", fields: [friend_id], references: [user_id])
+
+  @@id([friend_id, following_user_id])
+}
+
+model private_tasks {
+  private_task_id String    @id @default(cuid())
+  user_id         String
+  title           String?
+  description     String?
+  deadline        DateTime?
+  created_at      DateTime? @default(now())
+  done            Boolean?  @default(false)
+  users           users     @relation(fields: [user_id], references: [user_id])
+}
+
+model survey_answers {
+  answer_id  String    @id @default(cuid())
+  survey_id  String
+  answer     String?
+  created_at DateTime? @default(now())
+  surveys    surveys   @relation(fields: [survey_id], references: [survey_id])
+  votes      votes[]
+}
+
+model surveys {
+  survey_id      String           @id @default(cuid())
+  event_id       String
+  question       String?
+  created_at     DateTime?        @default(now())
+  events         events           @relation(fields: [event_id], references: [event_id])
+  survey_answers survey_answers[]
+  votes          votes[]
+}
+
+model user_event {
+  event_id  String
+  user_id   String
+  joined_at DateTime? @default(now())
+  events    events    @relation(fields: [event_id], references: [event_id])
+  users     users     @relation(fields: [user_id], references: [user_id])
+
+  @@id([event_id, user_id])
+}
+
+model users {
+  user_id                                  String          @id @default(cuid())
+  first_name                               String?
+  last_name                                String?
+  username                                 String          @unique
+  email                                    String?         @unique
+  password                                 String?
+  role                                     Role?
+  created_at                               DateTime?       @default(now())
+  last_login                               DateTime?       @default(now())
+  confirmed                                Boolean?        @default(false)
+  profile_color                            String?         @default("blue")
+  is_private                               Boolean?        @default(true)
+  event_tasks                              event_tasks[]
+  events                                   events[]
+  friends_friends_following_user_idTousers friends[]       @relation("friends_following_user_idTousers")
+  friends_friends_friend_idTousers         friends[]       @relation("friends_friend_idTousers")
+  private_tasks                            private_tasks[]
+  user_event                               user_event[]
+  votes                                    votes[]
+
+  @@index([first_name], name: "first_name_idx")
+  @@index([last_name], name: "last_name_idx")
+}
+
+model votes {
+  user_id        String
+  survey_id      String
+  voted_at       DateTime?      @default(now())
+  answer_id      String
+  survey_answers survey_answers @relation(fields: [answer_id], references: [answer_id])
+  surveys        surveys        @relation(fields: [survey_id], references: [survey_id])
+  users          users          @relation(fields: [user_id], references: [user_id])
+
+  @@id([survey_id, user_id])
+}
+
+enum EventType {
+  LOCAL
+  ONLINE
+}
+
+enum Role {
+  ADMIN
+  USER
+}
```



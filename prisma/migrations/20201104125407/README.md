# Migration `20201104125407`

This migration has been generated by Max Streitberger at 11/4/2020, 1:54:07 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201104123016..20201104125407
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Event {
   event_id                String       @id @default(cuid())
@@ -91,9 +91,9 @@
   survey_id  String    @id @default(cuid())
   event_id   String?
   question   String?
   created_at DateTime? @default(now())
-  Event      Event?    @relation(fields: [event_id], references: [event_id])
+  event      Event?    @relation(fields: [event_id], references: [event_id])
 }
 enum EventType {
   LOCAL
```



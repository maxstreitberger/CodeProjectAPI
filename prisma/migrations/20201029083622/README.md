# Migration `20201029083622`

This migration has been generated by Max Streitberger at 10/29/2020, 9:36:22 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."EventTask" DROP CONSTRAINT "EventTask_pkey",
DROP COLUMN "event_task_id",
ADD COLUMN "task_id" text   NOT NULL ,
ADD PRIMARY KEY ("task_id")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201029081852..20201029083622
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Event {
   event_id                String       @id @default(cuid())
@@ -50,9 +50,9 @@
   @@id([event_id, user_id])
 }
 model EventTask {
-  event_task_id     String      @id @default(cuid())
+  task_id     String            @id @default(cuid())
   event_id          String?
   title             String?
   description       String?
   deadline          String?
```



# Migration `20201029095607`

This migration has been generated by Max Streitberger at 10/29/2020, 10:56:07 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."EventTask" DROP CONSTRAINT "EventTask_pkey",
DROP COLUMN "task_id",
ADD COLUMN "event_task_id" text   NOT NULL ,
ADD PRIMARY KEY ("event_task_id")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201029095503..20201029095607
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Event {
   event_id                String       @id @default(cuid())
@@ -52,17 +52,17 @@
   @@id([event_id, user_id])
 }
 model EventTask {
-  task_id       String      @id @default(cuid())
-  event_id      String?
-  title         String?
-  description   String?
-  deadline      String?
-  created_at    DateTime?   @default(now())
-  user_id       String?
-  event         Event?      @relation(fields: [event_id], references: [event_id])
-  created_by    User?       @relation(fields: [user_id], references: [user_id])
+  event_task_id       String          @id @default(cuid())
+  event_id            String?
+  title               String?
+  description         String?
+  deadline            String?
+  created_at          DateTime?       @default(now())
+  user_id             String?
+  event               Event?          @relation(fields: [event_id], references: [event_id])
+  created_by          User?           @relation(fields: [user_id], references: [user_id])
 }
 model PrivateTask {
   private_task_id           String          @id @default(cuid())
```



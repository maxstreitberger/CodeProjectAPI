# Migration `20201113155135`

This migration has been generated by Max Streitberger at 11/13/2020, 4:51:35 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "User" ADD COLUMN     "confirmed" BOOLEAN NOT NULL DEFAULT false
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201104151256..20201113155135
--- datamodel.dml
+++ datamodel.dml
@@ -1,11 +1,12 @@
 generator client {
   provider = "prisma-client-js"
+  binaryTargets = ["native", "linux-musl"]
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Event {
   event_id                String       @id @default(cuid())
@@ -42,8 +43,10 @@
   friends       Friend[]      @relation("Friend_following_user_idToUser")
   followed_by   Friend[]      @relation("Friend_friend_idToUser")
   private_tasks PrivateTask[]
   joinedEvents  User_Event[]
+  votes         Vote[]
+  confirmed     Boolean        @default(false)
 }
 model User_Event {
   event_id  String
```



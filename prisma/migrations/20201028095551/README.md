# Migration `20201028095551`

This migration has been generated by Max Streitberger at 10/28/2020, 10:55:51 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
DROP TYPE "event_type"

DROP TYPE "role"

CREATE TABLE "public"."Event" (
"event_id" text   NOT NULL ,
"title" text   ,
"date" text   ,
"event_start" text   ,
"description" text   ,
"is_public" boolean   ,
"event_type" "EventType"  ,
"street_and_house_number" text   ,
"city" text   ,
"country" text   ,
"meeting_link" text   ,
"created_at" timestamp(3)   DEFAULT CURRENT_TIMESTAMP,
"host_id" text   ,
PRIMARY KEY ("event_id")
)

CREATE TABLE "public"."User" (
"user_id" text   NOT NULL ,
"first_name" text   ,
"last_name" text   ,
"username" text   ,
"email" text   ,
"password" text   ,
"role" "Role"  ,
"created_at" timestamp(3)   DEFAULT CURRENT_TIMESTAMP,
"last_login" timestamp(3)   DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("user_id")
)

CREATE UNIQUE INDEX "User.email_unique" ON "public"."User"("email")

ALTER TABLE "public"."Event" ADD FOREIGN KEY("host_id")REFERENCES "public"."User"("user_id") ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201028095551
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,49 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+model Event {
+  event_id                String      @id @default(cuid())
+  title                   String?
+  date                    String?     @default(dbgenerated())
+  event_start             String?     @default(dbgenerated())
+  description             String?
+  is_public               Boolean?
+  event_type              EventType?
+  street_and_house_number String?
+  city                    String?
+  country                 String?
+  meeting_link            String?
+  created_at              DateTime?   @default(now())
+  host_id                 String?
+  host                    User?       @relation(fields: [host_id], references: [user_id])
+}
+
+model User {
+  user_id    String    @id @default(cuid())
+  first_name String?
+  last_name  String?
+  username   String?
+  email      String?   @unique
+  password   String?
+  role       Role?
+  created_at DateTime? @default(now())
+  last_login DateTime? @default(now())
+  events     Event[]
+}
+
+enum EventType {
+  LOCAL
+  ONLINE
+}
+
+enum Role {
+  ADMIN
+  USER
+}
+
```



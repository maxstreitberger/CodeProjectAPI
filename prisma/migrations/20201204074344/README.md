# Migration `20201204074344`

This migration has been generated by Max Streitberger at 12/4/2020, 8:43:44 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201204073901..20201204074344
--- datamodel.dml
+++ datamodel.dml
@@ -4,9 +4,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model cities {
   name       String    @id
@@ -61,10 +61,10 @@
 model friends {
   follows_since                          DateTime? @default(now())
   following_user_id                      String
   friend_id                              String
-  users_friends_following_user_idTousers users     @relation("friends_following_user_idTousers", fields: [following_user_id], references: [user_id])
-  users_friends_friend_idTousers         users     @relation("friends_friend_idTousers", fields: [friend_id], references: [user_id])
+  following_user                         users     @relation("friends_following_user_idTousers", fields: [following_user_id], references: [user_id])
+  friend                                 users     @relation("friends_friend_idTousers", fields: [friend_id], references: [user_id])
   @@id([friend_id, following_user_id])
 }
```



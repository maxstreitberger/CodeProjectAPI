# Migration `20201029073822`

This migration has been generated by Max Streitberger at 10/29/2020, 8:38:22 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201029073647..20201029073822
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Event {
   event_id                String       @id @default(cuid())
@@ -50,15 +50,15 @@
   @@id([event_id, user_id])
 }
 model Task {
-  event_task_id     String    @id @default(cuid())
-  event_id    String?
-  title       String?
-  description String?
-  deadline    String?
-  created_at  DateTime? @default(now())
-  Event       Event?    @relation(fields: [event_id], references: [event_id])
+  event_task_id     String      @id @default(cuid())
+  event_id          String?
+  title             String?
+  description       String?
+  deadline          String?
+  created_at        DateTime?   @default(now())
+  event             Event?      @relation(fields: [event_id], references: [event_id])
 }
 enum EventType {
   LOCAL
```



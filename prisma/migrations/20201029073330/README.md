# Migration `20201029073330`

This migration has been generated by Max Streitberger at 10/29/2020, 8:33:30 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201028153758..20201029073330
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Event {
   event_id                String       @id @default(cuid())
@@ -21,8 +21,9 @@
   meeting_link            String?
   created_at              DateTime?    @default(now())
   host_id                 String?
   host                    User?        @relation(fields: [host_id], references: [user_id])
+  Task                    Task[]
   joinedEvents            User_Event[]
 }
 model User {
@@ -48,8 +49,18 @@
   @@id([event_id, user_id])
 }
+model Task {
+  task_id     String    @id @default(cuid())
+  event_id    String?
+  title       String?
+  description String?
+  deadline    String?
+  created_at  DateTime? @default(now())
+  Event       Event?    @relation(fields: [event_id], references: [event_id])
+}
+
 enum EventType {
   LOCAL
   ONLINE
 }
```



# Migration `20201126074307`

This migration has been generated by Max Streitberger at 11/26/2020, 8:43:07 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "Event" ALTER COLUMN "title" DROP NOT NULL,
ALTER COLUMN "event_start" DROP NOT NULL,
ALTER COLUMN "is_public" DROP NOT NULL,
ALTER COLUMN "event_type" DROP NOT NULL,
ALTER COLUMN "date" DROP NOT NULL
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201123160116..20201126074307
--- datamodel.dml
+++ datamodel.dml
@@ -4,18 +4,18 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Event {
   event_id                String       @id @default(cuid())
-  title                   String
-  event_start             String
+  title                   String?
+  event_start             String?
   description             String?
-  is_public               Boolean
-  event_type              EventType
+  is_public               Boolean?
+  event_type              EventType?
   street_and_house_number String?
   city_id                 String?
   city                    City?        @relation(fields: [city_id], references: [name])
   // city                    String?
@@ -26,9 +26,9 @@
   created_at              DateTime?    @default(now())
   host_id                 String
   zip                     String?
   event_color             String       @default("green")
-  date                    DateTime
+  date                    DateTime?
   host                    User         @relation(fields: [host_id], references: [user_id])
   tasks                   EventTask[]
   surveys                 Survey[]
   guests                  User_Event[]
```



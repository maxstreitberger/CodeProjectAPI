# Migration `20201023132313`

This migration has been generated by Max Streitberger at 10/23/2020, 3:23:13 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Event" DROP CONSTRAINT "Event_host_id_fkey"

ALTER TABLE "public"."Event" DROP CONSTRAINT "Event_pkey",
DROP COLUMN "event_id",
ADD COLUMN "event_id" text   NOT NULL ,
DROP COLUMN "host_id",
ADD COLUMN "host_id" text   ,
ADD PRIMARY KEY ("event_id")

ALTER TABLE "public"."User" DROP CONSTRAINT "User_pkey",
DROP COLUMN "user_id",
ADD COLUMN "user_id" text   NOT NULL ,
ADD PRIMARY KEY ("user_id")

ALTER TABLE "public"."Event" ADD FOREIGN KEY ("host_id")REFERENCES "public"."User"("user_id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER INDEX "public"."User_email_key" RENAME TO "User.email_unique"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201023132313
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,48 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+model Event {
+  event_id                String      @id @default(cuid())
+  title                   String?
+  date                    DateTime?   @default(dbgenerated())
+  event_start             DateTime?   @default(dbgenerated())
+  description             String?
+  is_public               Boolean?
+  event_type              event_type?
+  street_and_house_number String?
+  city                    String?
+  country                 String?
+  meeting_link            String?
+  created_at              DateTime?   @default(now())
+  host_id                 String?
+  User                    User?       @relation(fields: [host_id], references: [user_id])
+}
+
+model User {
+  user_id    String    @id @default(cuid())
+  first_name String?
+  last_name  String?
+  username   String?
+  email      String?   @unique
+  password   String?
+  role       role?
+  created_at DateTime? @default(now())
+  last_login DateTime? @default(now())
+  Event      Event[]
+}
+
+enum event_type {
+  LOCAL
+  ONLINE
+}
+
+enum role {
+  ADMIN
+  USER
+}
```



# Migration `20201104092703`

This migration has been generated by Max Streitberger at 11/4/2020, 10:27:03 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201104082310..20201104092703
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Event {
   event_id                String       @id @default(cuid())
@@ -37,10 +37,10 @@
   created_at                      DateTime?     @default(now())
   last_login                      DateTime?     @default(now())
   events                          Event[]
   event_tasks                     EventTask[]
-  followed_by                     Friend[]      @relation("Friend_follower_idToUser")
-  is_following                    Friend[]      @relation("Friend_user_idToUser")
+  followed_by                     Friend[]      @relation("Friend_user_idToUser")
+  friends                         Friend[]      @relation("Friend_following_user_idToUser")
   private_tasks                   PrivateTask[]
   joinedEvents                    User_Event[]
 }
@@ -79,10 +79,10 @@
 model Friend {
   user_id                       String
   follower_id                   String
   follows_since                 DateTime? @default(now())
-  follower                      User      @relation("Friend_follower_idToUser", fields: [follower_id], references: [user_id])
-  user                          User      @relation("Friend_user_idToUser", fields: [user_id], references: [user_id])
+  user                          User      @relation("Friend_user_idToUser", fields: [follower_id], references: [user_id])
+  following_user                User      @relation("Friend_following_user_idToUser", fields: [user_id], references: [user_id])
   @@id([user_id, follower_id])
 }
```


